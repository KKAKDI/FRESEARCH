1. DB 테이블 재구성
- 관심카테고리, 알림 테이블의 FK값인 이메일이 현재 NULL인데 UNIQUE NOT NULL로 설정할지
- 알림 테이블에 컬럼 추가할지(열람여부, 열람일, 발신일 등)
- 코드값 숫자가 아닌 영문으로 할거면 시퀀스 증가는 어떻게 할것인지

2. 기능 구현
- 시큐리티 로그인과 알림 기능 어떻게 구현 할 것인지 생각

3. 코드 랜덤 생성 영문 + 숫자 합칠수있는지

페이지명 : research_list.jsp